apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: cldinf-app
  name: cldinf-ingress-backend
spec:
  rules:
  - host: "10.105.227.5"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: cldinf-api
            port:
              number: 80